class UsuarioModel {  constructor() {    this.usuarios = [      { id: 1, usuario: "admin", rol: "admin", contrasena: "1234" },      { id: 2, usuario: "gerente", rol: "gerente", contrasena: "gerente123" }    ];  }  getUsuarios() {    return this.usuarios;  }  getUsuarioById(id) {    return this.usuarios.find(u => u.id === id);  }  agregarUsuario(usuario) {    this.usuarios.push(usuario);  }  actualizarUsuario(id, usuario, rol, contrasena) {    const u = this.getUsuarioById(id);    if (u) {      u.usuario = usuario;      u.rol = rol;      u.contrasena = contrasena;    }  }  eliminarUsuario(id) {    this.usuarios = this.usuarios.filter(u => u.id !== id);  }}window.usuarioModel = new UsuarioModel();